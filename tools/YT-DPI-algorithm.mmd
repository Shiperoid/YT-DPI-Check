%%{init: {'theme': 'base', 'themeVariables': { 'fontSize': '13px'}}}%%
flowchart TB
  S["<b>Старт</b><br/>UTF-8, лог, QuickEdit • глобалы • Add-Type TlsScanner + AdvancedTraceroute"]

  E["<b>ENGINE</b><br/>Load-Config + DnsCache • заглушка UI → Get-Targets → Draw-UI<br/>Get-NetworkInfo → Targets+Draw-UI • при устаревшем кэше повтор сети<br/>RunCount%10=0 → Check-Update → подсказка U"]

  N["<b>Get-NetworkInfo</b><br/>DNS WMI • CDN redirector+прокси • GEO кэш 24ч или цепочка API • IPv6 если не только-IPv4"]

  L["Read-MainLoopKey"]

  H{"Клавиша?"}

  SUB["<b>Не Enter</b><br/>Q/Esc Stop+Save • H Help • S Settings • P Proxy <i>(смена GEO→сеть+targets)</i><br/>R Report • U Update GitHub • D Deep trace по номеру • T TestProxy"]

  EN["<b>Enter — скан</b><br/>Ping/TCP 8.8.8.8 fail→continue • NetCache из конфига<br/>кэш&gt;10мин/Loading→фон NetInfoUpdater • Get-Targets+Draw-UI<br/>Start-ScanWithAnimation → LastScan+Draw • job→обновить ISP"]

  SC["<b>Скан</b> Pool+$Worker • фаза1 Q/Esc abort • фаза2 reveal строк<br/>устарел NetInfo→sync • все IP BLOCK+HasIPv6→сброс v6+AAAA кэш"]

  WK["<b>Воркер</b> DNS/PROXIED→HTTP80→ERR=IP BLOCK<br/>TLS1.3+retry • TLS1.2+retry • матрица: AVAILABLE/THROTTLED/DPI RESET/DPI BLOCK/IP BLOCK"]

  F["Draw-UI, статус, Clear-KeyBuffer → снова цикл"]

  X["Stop-Script"]

  S --> E
  E -.-> N
  E --> L
  L --> H
  H -->|Enter| EN
  H -->|другое| SUB
  H -->|Q Esc| X
  SUB --> L
  EN -.-> SC
  EN -.-> WK
  EN --> F
  F --> L
