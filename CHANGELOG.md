# YT-DPI v2.3.3 (Windows: `YT-DPI.ps1` + `YT-DPI.bat`)

**Сравнение в Git:** [2.3.2…2.3.3](https://github.com/Shiperoid/YT-DPI/compare/2.3.2...2.3.3) (после публикации тега `2.3.3`; до тега — [2.3.2…master](https://github.com/Shiperoid/YT-DPI/compare/2.3.2...master)).

Релиз **только для Windows‑комплекта** (`.bat` + `.ps1`): управление **пользовательским списком целей** через файл **`targets.txt`** и пункты меню **[S]**.

### Добавлено

* **`targets.txt` рядом со скриптом.** При включённой опции в конфиге скрипт читает список доменов из этого файла (UTF‑8): пустые строки и строки, начинающиеся с `#`, игнорируются; принимаются непустые строки, в которых есть хотя бы одна `.` (остальное отбрасывается с записью в отладочный лог).
* **Конфиг `UseCustomTargets`.** Переключатель **[S] → 6**: если **ВКЛ** и файл существует и содержит валидные строки — скан идёт по файлу; если **ВЫКЛ** — используется встроенный список YouTube/Google (как раньше).
* **[S] → 7 — экспорт текущих целей в `targets.txt`.** Перезапись файла, UTF‑8 без BOM; после успешного экспорта **`UseCustomTargets`** принудительно включается и список перечитывается.
* **Авто‑инициализация `UseCustomTargets`.** Если в сохранённом конфиге поля ещё не было — при первом запуске выставляется **`true`**, если **`targets.txt`** уже лежит рядом со скриптом (иначе можно включить вручную в **[S]**).
* **`Initialize-Targets` и расширенный заголовок отладочного лога.** В **`YT-DPI_Debug.log`** в блоке старта сессии фиксируется, откуда взят список целей (кастомный файл или встроенный список), и даётся примечание про возможное дополнение CDN для встроенного режима.

### Изменено

* **`Get-Targets`:** автоматическое добавление **`NetInfo.CDN`** к списку выполняется **только** когда цели **не** загружены из кастомного файла (в режиме кастомного файла список не изменяется CDN‑подмесом).
* **Меню [S]:** пункт **6** занят переключением **`targets.txt`**; экспериментальный **параллельный первый проход TLS** (`ScanParallelTlsFirstPass`) по‑прежнему читается из **`config.json`** и участвует в воркере скана, но **в этом меню больше не переключается** — при необходимости флаг можно выставить вручную в конфиге.

### Совместимость

* Старые конфиги без **`UseCustomTargets`** дополняются автоматически (см. выше).
* Формат отчёта **`YT-DPI_Report.txt`**, прокси и TLS‑движок без изменений контракта.

### Как обновиться с 2.3.2

* Замените **`YT-DPI.bat`** и **`YT-DPI.ps1`** из [Releases](https://github.com/Shiperoid/YT-DPI/releases) (тег **`2.3.3`**, когда опубликован) или через **`[U] UPDATE`**, когда GitHub покажет эту версию последней.

---

### `YT-DPI.sh` (Bash) в выпуске 2.3.3

**Номер версии в `YT-DPI.sh` по-прежнему 2.3.2; пользовательских изменений для Bash в этом патче нет** — синхронизация под 2.3.3 ожидается отдельно.

---

# YT-DPI v2.3.2 (Windows: `YT-DPI.ps1` + `YT-DPI.bat`)

**Сравнение в Git:** [2.3.1…2.3.2](https://github.com/Shiperoid/YT-DPI/compare/2.3.1...2.3.2) (после публикации тега `2.3.2`; до тега — [2.3.1…master](https://github.com/Shiperoid/YT-DPI/compare/2.3.1...master)).

Релиз сосредоточен на **скорости скана**, **снижении нагрузки на консоль** и **внутреннем рефакторинге** TLS‑логики. Все таймауты вынесены в один блок констант `$SCRIPT:CONST` — там же видны старые значения в комментариях «Снижено с …», поэтому при необходимости можно вернуться к консервативным таймингам, поправив одну таблицу.

### Быстрее (главное в этом релизе)

Массовое снижение таймаутов и пауз через **единую таблицу** `$SCRIPT:CONST` ([`YT-DPI.ps1`](YT-DPI.ps1), верхний блок):

* **Базовые сетевые:** `TimeoutMs` **1500 → 700**, `ProxyTimeout` **2500 → 1200**, `Mutex.WaitMs` **15000 → 7000**.
* **Скан:** `HttpDirectCapMs` **1200 → 600**, `TlsFastMsDirect` **1600 → 800**, `TlsFastMsProxy` **2200 → 1200**, `TlsRetryMsDirect/ProxyFloor` **2600 → 1300**.
* **NetInfo (определение ISP/CDN/DNS):** `WebFastDefaultMs` **3000 → 1000**, `RedirectorMs` **2000 → 700**, `GeoPerRequestMs` **1500 → 500**, `Ipv6ProbeWaitMs` **1000 → 350**, `RedirectorRequestMs` **3000 → 700**.
* **Deep Trace:** `DefaultTimeoutSec` **5 → 2**, `HopTcpTlsTimeoutSec` **2 → 1**, `HopTlsCapMs` **3000 → 1200**, `HopTcpCapMs` **2000 → 800**, `TracertHopWaitMs` **350 → 120**, `TraceProcessKillMsBase` **12000 → 5000**, `TraceProcessKillMsPerHop` **1400 → 400**, `TcpPollSliceMs` **90 → 40**, `UdpRecvPollMs` **1000 → 300**.
* **Самотест прокси `[T]`:** `Tunnel443Ms` **7000 → 2000**, `HttpGstaticMs` **5000 → 1300**, `TcpToProxyMs` **4000 → 1500**, `QuickTunnelMs` **5000 → 2000**, `DetectTcpConnectMs` **2000 → 700**, `DetectStreamRwMs` **2000 → 800**, `HttpSlowWarnMs` **4800 → 1500**; внутренние паузы между фазами `120/150/200ms → 60/60/80ms`.
* **Internet probe (перед сканом):** `PingTimeoutMs` **1000 → 400**, `TcpFallbackMs` **1000 → 400**.
* **HTTP misc через прокси:** `GitHubReleaseApiMs` **5000 → 1500**, `RedirectorViaProxyMs` **3000 → 1200**, `GeoProviderViaProxyMs` **1500 → 500**.

**Что это даёт на практике.** На стабильном интернете весь скан и Deep Trace заметно быстрее «холодным» прогоном. Логика **retry с увеличенным таймаутом сохранена**: если первый быстрый проход TLS даёт `DRP`, скрипт автоматически повторяет с увеличенным окном (`TlsRetryMsDirect/ProxyFloor`) — ложных вердиктов на медленных каналах не прибавилось.

### Меньше нагрузки на консоль и плавнее анимация

* **Троттлинг строки прогресса в фазе сбора результатов:** `StatusBarThrottleCollectMs` **240 → 90 ms** — показатель `[X/Y]` теперь обновляется заметно быстрее без перерисовки таблицы.
* **Троттлинг в фазе «водопада»:** `StatusBarThrottleRevealMs` **280 → 110 ms**.
* **Отдельный FPS «водопада»:** `RevealAnimFps = 48` (общий `AnimFps = 30` остался для остальной анимации). Ровные кадры при выводе уже готовых строк.

### Добавлено

* **Параллельный первый проход TLS (эксперимент, по умолчанию ВЫКЛ).** Новый пункт **6** в меню **`[S]`**: «Параллельный первый проход TLS (Auto, T13+T12)». В Auto‑режиме первая проба `TLS 1.3` и `TLS 1.2` идёт одновременно через `[System.Threading.Tasks.Task]::Run`, не суммируя таймауты. При сбое `Task.WaitAll` (PS5.1, нестандартный SynchronizationContext и т.п.) — **автоматический откат** на последовательный путь без ложного `IP BLOCK`. Конфиг‑поле `ScanParallelTlsFirstPass` хранится между запусками.
* **Принудительный сброс «сломанного» эксперимента в существующих конфигах.** Новая миграция `Initialize-DisableBrokenParallelTlsTasks` ставит метку `ParallelTlsTasksDisabled032026 = true` и принудительно `ScanParallelTlsFirstPass = false` при первом запуске **2.3.2** — у пользователей с поломанным значением флаг гарантированно начнёт жизнь с «ВЫКЛ».
* **«Двусторонний» вердикт в одиночных режимах TLS.** В режимах `TlsMode = TLS12` и `TlsMode = TLS13`, если основная колонка показала `RST`/`DRP`, скрипт **тихо** (без вывода в таблицу) проверяет другую версию TLS и присваивает корректный вердикт: `THROTTLED`, `AVAILABLE`, `DPI RESET` или `DPI BLOCK`. Раньше в одиночных режимах вердикт сводился к плоскому `DPI BLOCK`/`AVAILABLE`. Описание поведения добавлено в подсказки пунктов **3** меню `[S]`.
* **Хелперы `Set-Verdict-DualTlsCells` и `Invoke-Tls12HandshakeOnce`.** Логика вердикта и handshake TLS 1.2 вынесены в отдельные функции внутри воркера — единая точка для исправлений.
* **`CONST`‑параметры пула воркеров.** `ScanPoolMinWorkers = 8`, `ScanPoolDirectMax = 24`, `ScanPoolProxyMax = 12`, `ScanPoolCpuMultiplier = 3`. Поведение прежнее (`8…24`, при прокси `≤ 12`, не больше числа целей), но теперь крутится через таблицу констант.

### Обновлено

* **`Worker` принимает `[bool]$ParallelTlsFirstPass`.** `Start-ScanWithAnimation` пробрасывает текущее значение из конфига в каждый runspace — без этого новая опция меню была бы «декоративной».
* **Все ранее «зашитые» лимиты** — `15000` ms у mutex, `1200/1600/2200/2600` ms TLS, `240/280` ms UI‑троттлинг — читаются из `$CONST`. Единая точка для тюнинга и тестов.
* **Фоновое задание `Start-QuickNetInfoUpdater`** теперь принимает `mutexWaitMs` параметром (передаётся из `$CONST.Mutex.WaitMs`); внутри job‑скриптблока больше нет «магической» `15000`.
* **Описание режимов TLS в меню `[S]`** переписано: явно показано, что в `TLS12`/`TLS13` теневая проверка другой версии используется **только** для уточнения вердикта, а соответствующая колонка остаётся `N/A`.

### Исправлено

* **Дубликат TLS 1.2 handshake** (Auto / TLS12 / retry) свёрнут в `Invoke-Tls12HandshakeOnce`. Ранее три похожих блока с `BeginAuthenticateAsClient`/`AsyncWaitHandle` могли немного расходиться по набору `catch`‑паттернов («reset / сброс / forcibly / closed / разорвано / failed»), теперь единый источник правды.
* **Логика вердикта в Auto** свёрнута в `Set-Verdict-DualTlsCells` — раньше она дублировалась тремя ветками (`-not consider13`, `-not consider12`, оба), теперь правится в одном месте.

### Совместимость

* **Поведение по умолчанию не меняется.** Первый проход TLS остаётся **последовательным** (T13 → T12) — это проверенная ветка для **Windows PowerShell 5.1**. Параллельный путь включается **только** через **`[S] → 6`** и сохраняется в конфиге.
* **Существующие `config.json`** читаются без миграции; новые поля `ScanParallelTlsFirstPass` и метка `ParallelTlsTasksDisabled032026` добавляются автоматически со значениями «ВЫКЛ» / `true`.
* Никакого изменения внешнего API: формат отчёта **`YT-DPI_Report.txt`**, протоколы прокси (HTTP/SOCKS5), формат `YT-DPI_Debug.log` — всё прежнее.
* **Совместимо с PS 5.1 и PS 7.** Для проверки именно сборки `Add-Type` встроенных C#‑блоков по матрице PS перед релизом прогоняется `release-gate` (см. ниже и **[README.md](README.md)**).

### Проект

* Расширен скрипт **[`tools/release-gate.ps1`](tools/release-gate.ps1)** и добавлены вспомогательные:
  * **[`tools/release-gate.cmd`](tools/release-gate.cmd)** и **[`tools/release-gate-quick.cmd`](tools/release-gate-quick.cmd)** — CMD‑обёртки для запуска без знания PowerShell‑параметров.
  * **[`tools/release-gate-addtype.ps1`](tools/release-gate-addtype.ps1)** — извлекает `$tlsCode` / `$traceCode` here‑string из `YT-DPI.ps1` и вызывает `Add-Type` в указанном PowerShell‑процессе.
  * **[`tools/smoke-yt-dpi-engines.ps1`](tools/smoke-yt-dpi-engines.ps1)** — пост‑компиляционный smoke (AST + короткие паттерны runspace).
  * **[`tools/smoke-yt-dpi-sh.sh`](tools/smoke-yt-dpi-sh.sh)** — `bash -n` для `YT-DPI.sh` (синтаксический smoke в CI).
* **CI:** workflow [`.github/workflows/release-gate.yml`](.github/workflows/release-gate.yml) теперь использует `paths`‑фильтр на `push`, а `pull_request` идёт без фильтра — required‑чек больше не «висит» в ожидании.

### Как обновиться с 2.3.1

* Запускайте **`YT-DPI.bat`** (он задаёт `-ExecutionPolicy Bypass`).
* Внутри программы нажмите **`[U] UPDATE`**, когда GitHub отдаст релиз `2.3.2` как «последний». Файл `YT-DPI.bat` и/или `YT-DPI.ps1` обновятся через временные процессы; вторая часть комплекта докачается рядом.
* Либо вручную возьмите оба файла со страницы [Releases](https://github.com/Shiperoid/YT-DPI/releases) и положите в одну папку.

---

---

# YT-DPI v2.3.2 (Bash, `YT-DPI.sh`)

**Базис сравнения:** `YT-DPI.sh` из релиза **`2.3.1`** → `YT-DPI.sh` версии **`2.3.2`**. Внутренний движок повышен: **`Barebuh Pro v2.3.4` → `v2.3.7`**.

Главное в релизе — **параллельная и более экономная Bash‑реализация**: одновременные TLS 1.2/1.3, лимит фоновых воркеров для слабых роутеров, кросс‑скриптовый кэш гео и нормальный многоплатформенный детектор локального DNS.

### Параллелизм и производительность

* **Лимит фоновых воркеров** `SCAN_MAX_JOBS` (env‑override `YT_DPI_MAX_JOBS`): авто **`12`** для Git Bash/MSYS, **`6`** для OpenWrt/Entware (по эвристикам `/jffs`, `/overlay`, `/rom`, `OPENWRT_RELEASE`, `/opt/etc/opkg`), **`0`** (без лимита) для обычных Linux/macOS. Цикл скана дожидается освобождения слота через `wait -n` — слабые роутеры больше не получают залп из 25 одновременных `curl`.
* **Параллельные TLS 1.2 + TLS 1.3 в Auto.** Внутри воркера обе пробы идут одновременно `&` + `wait $pid12 / $pid13`, не суммируя таймауты. Раньше шли последовательно — на каждую цель уходило `tls12_timeout + tls13_timeout`.
* **Удалена анимация `SCANNING …` / волны LAT.** Кадры `FRAMES` / `LAT_WAVE` / счётчик `f` / `ANIM_EVERY` убраны — больше не перерисовываем целевые строки на каждом тике; вместо этого один статический `PREPARING…`, и строка обновляется по факту прихода `*.res`. На роутерах и в Git Bash «лесенки» в строке LAT исчезают, а CPU‑нагрузка падает.
* **Перерисовка по сигнатуре.** Новая функция `ui_state_sig` собирает «отпечаток» состояния (DNS / CDN / ISP / LOC / прокси / IPv6 / цели / IP‑pref / TLS‑mode); после `[Enter]` `draw_ui` вызывается **только если** отпечаток изменился — нажатия Enter подряд больше не моргают полным переисованием.
* **`READ_TIMEOUT` для MSYS** поднят до `0.2 s` (env `YT_DPI_READ_TIMEOUT`) — без анимации цикл не нужно так часто будить.

### Добавлено

* **Кросс‑скриптовый кэш гео** `~/.config/yt-dpi/geo_cache.json`. Тики `.NET` совместимы по формату с `YT-DPI.ps1` — общий кэш ISP/LOC. TTL 24 часа, ключ — состояние прокси (`direct` либо `TYPE|HOST:PORT`). Повторный старт мгновенный, меньше внешних запросов.
* **Цепочка GEO‑провайдеров (как в `Get-NetworkInfo` PS1):** `ip-api.com → ifconfig.co → ipapi.co → ipwhois.app → ipinfo.io`, с нормализацией ISP `_strip_isp_suffix` (отрезает `LLC`, `Inc`, `Ltd`, `OOO`, `JSC`, `Group`, `Corporation`, …). Цепочка работает только при наличии **`jq`**; без `jq` остаётся прежний fallback на `ip-api.com/line/`.
* **Надёжное определение локального DNS (`detect_local_dns`):**
  * **macOS** — `scutil --dns`, первый `nameserver[N]`;
  * **Linux с systemd‑resolved** — `resolvectl status` (`Current DNS Server` → `DNS Servers`), затем `/run/systemd/resolve/resolv.conf` (раньше отдавал stub `127.0.0.53`);
  * **NetworkManager** — `nmcli dev show`, поле `IP4.DNS`;
  * **OpenWrt** — `uci get network.wan.dns` / `network.@dnsmasq[0].server`;
  * **Android / Termux** — `getprop net.dns1` / `dhcp.wlan0.dns1`;
  * **Windows Git Bash / MSYS / Cygwin** — `ipconfig.exe /all` через `MSYS2_ARG_CONV_EXCL`, с фильтром `169.254.*` и `127.*`;
  * **Общий `/etc/resolv.conf`** как последний fallback.
* **Резолв CDN как в PS1.** Теперь дёргается `redirector.googlevideo.com/report_mapping?di=no&nocache=…`, разбирается короткий код (`=> <short>`) и складывается `r1.<short>.googlevideo.com` (или полный `*.googlevideo.com`). Раньше пробивались статические префиксы `r1..r3`, `rr1..rr5` через `getent`/`nslookup` — у многих провайдеров это не работало (без рабочего обратного резолва).
* **Capability‑детект curl на старте.** Флаги `CURL_HAS_TLS13` (`--tlsv1.3`), `CURL_HAS_TLS_MAX` (`--tls-max`), `CURL_HAS_SOCKS5H` (`socks5h://`) определяются по `curl --help all`. Если флага нет — соответствующая колонка показывается как `N/A`, а не как `DRP`/`FAIL`. Решает типичный сценарий с busybox‑curl на роутерах.
* **`proxy_url_for_curl`** — корректное преобразование `socks5://` ↔ `socks5h://` **только** если curl поддерживает `socks5h` (иначе оставляем `socks5://`, чтобы не получить «scheme not supported»).
* **Полный TUI‑стек:** `tui_enter` / `tui_leave`, переход в alt‑screen только при TTY, корректный `cleanup` (не падает при пустом `TMP_DIR`). Меню Help / Settings / Proxy / Test теперь явно вызывают `tui_leave` при входе и `tui_enter` при выходе — после возврата нет «зависших» строк ANSI.
* **`REMOTE_IP` из curl.** В HTTP‑пробе передаётся `-w "%{remote_ip}"`, IP цели берётся из реального соединения. Resolver вызывается только если `REMOTE_IP` пустой / `0.0.0.0` / `::`.
* **Корректный парсинг `nslookup` в `resolve_target_ip`.** Проход по блокам `Name:` → `Address(es): IPv4`, а не по первому `Address:` (последний под Windows‑Git‑Bash часто отдавал адрес DNS‑сервера, а не цели). Резерв через `ping` теперь читает и Linux‑формат `PING host (1.2.3.4)`, и Windows‑формат `Pinging host [1.2.3.4]`.
* **IPv6‑детект.** `ping6` с разным набором флагов под macOS/Linux + `curl -6` к **`cloudflare.com`** **и** `ipv6.google.com`. Раньше пробивался только `ipv6.google.com` — у пользователей с заблокированной TLS 1.3 к Google скрипт ошибочно решал «нет IPv6».
* **Меню прокси:** информативное сообщение, если запросили несуществующую запись истории (раньше тихо ничего не происходило). Все ветки меню парные по `tui_leave` / `tui_enter`.
* **Обновлён движок** в баннере: `Barebuh Pro v2.3.4` → **`v2.3.7`**.
* **Подровнен пробел** в строке HELP: `THROTTLED    ` → `THROTTLED     ` — теперь столбцы помощника ровные.

### Убрано

* **Кадры анимации** `FRAMES` / `LAT_WAVE` / `ANIM_EVERY`, счётчик `f` и связанные `out_str` каждый тик. Они стоили дороже, чем давали — при параллельных проверках и быстром приходе результатов анимация только мешала.
* **Массив `CDN_LIST`** — заменён на одиночный `CDN`, как в PS1.
* **Жёстко зашитый wrapper `/usr/bin/curl`.** Теперь используется `command -v curl` и переменная `CURL_BIN`. Работает там, где curl лежит в `/opt/bin/` (Entware), `/usr/local/bin/` (macOS Homebrew), MSYS‑путях.
* **Наивный `grep nameserver /etc/resolv.conf | head -1`** — заменён полноценным `detect_local_dns`.

### Исправлено

* **Меню Help / Settings / Proxy / Test** больше не оставляют alt‑буфер «полуоткрытым» при возврате — экран не «прыгает» и не печатает поверх рамки.
* **macOS:** `ping6` без `-W` (Darwin использует другой ключ), отдельная ветка детекта `OS_MAC`.
* **Пустой `TMP_DIR`** больше не передаётся в `rm -rf` в `cleanup` (защита от ранних ошибок до `mktemp`).
* **`nslookup` под Windows‑Git‑Bash** больше не возвращает адрес DNS‑сервера как «найденный IP» цели.
* **Усечение ISP до 30 символов** теперь делается **в одном месте** (`get_network_info`), а не в `get_network_info` и `draw_ui` параллельно с разной длиной.

### Совместимость

* **busybox‑curl на Entware/OpenWrt** без `--tls-max` или `socks5h://` поддержан корректно: соответствующие колонки покажут `N/A`, а не `DRP`/`FAIL`. Сам скан на таких прошивках теперь не «ломается».
* **Профиль `MSYSTEM` (Git Bash)** подхватывается автоматически: лимит воркеров `12`, повышенный `READ_TIMEOUT`.
* **Конфиг `~/.config/yt-dpi/config.json`** со старых версий читается без миграции (нужен `jq`); кэш гео — отдельный файл `geo_cache.json` рядом, ничего не пересекается.
* **Engine `v2.3.7`** в баннере — это **внутренний** номер ядра обработки; на формат `YT-DPI_Report.txt`, `config.json` и поведение CLI это не влияет.
* **Bash 3 / Bash 4+:** работают оба, на Bash 3 точные таймауты `read` грубее (`READ_TIMEOUT="1"`), как и раньше.

### Релиз на GitHub (v2.3.2)

Для Linux / macOS / Git Bash / Entware скачайте **`YT-DPI.sh`** со [страницы релиза v2.3.2](https://github.com/Shiperoid/YT-DPI/releases/tag/2.3.2).

Запуск одной строкой (нужен `bash`):

```bash
bash <(curl -Ls "https://raw.githubusercontent.com/Shiperoid/YT-DPI/2.3.2/YT-DPI.sh")
```

---

# YT-DPI v2.3.1 (hotfix)

**Сравнение в Git:** [2.3.0…2.3.1](https://github.com/Shiperoid/YT-DPI/compare/2.3.0...2.3.1) (после публикации тега `2.3.1`).

### Исправлено

* **Windows PowerShell 5.1 (`powershell.exe`):** встроенный C# для TLS‑сканера и трассировки снова **компилируется через `Add-Type`**. В 2.3.0 использовались конструкции и API, которые старый компилятор/рантайм 5.1 не принимает (`Random.Shared`, интерполяция `$"..."`, `?.` и др.) — из‑за этого у части пользователей в статусе было **«TLS scanner failed to load»**, а скан по **Enter** фактически не запускался. В 2.3.1 код приведён к совместимости с **Desktop 5.1** и проверен под **pwsh**.
* **Процесс релиза:** добавлены **`tools/release-gate.ps1`** (извлечение here-string из `YT-DPI.ps1` + **`Add-Type`** в процессах **5.1 x64**, при наличии **WOW64 5.1**, во всех найденных **`pwsh`**, после компиляции — **`smoke-yt-dpi-engines.ps1`** в том же процессе; опционально **`YT_DPI_GATE_EXTRA_PS`**) и GitHub Actions **`release-gate`** с матрицей образов Windows и версий **pwsh** через **`PSModule/install-powershell`**, чтобы регрессии встроенного C# не проходили незамеченными. Подробности запуска — в **[README.md](README.md)** (раздел про **`release-gate`**).

### Как обновиться с 2.3.0

* Запускайте **`YT-DPI.bat`** (он задаёт **`-ExecutionPolicy Bypass`**). Если открываете только **`YT-DPI.ps1`**, при строгой политике подписи Windows может отказать в запуске — тогда снова **`.bat`** или вручную: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\YT-DPI.ps1`.
* В программе нажмите **`[U]` UPDATE**, если GitHub уже показывает релиз **2.3.1** новее вашей версии (проверка идёт через API **последнего релиза**; файлы подтягиваются с ветки **`master`** — важно, чтобы туда уже попал хотфикс).
* Либо скачайте **`YT-DPI.ps1`** и при необходимости **`YT-DPI.bat`** со [страницы Releases](https://github.com/Shiperoid/YT-DPI/releases) и положите в одну папку.

### Текст для вставки в описание GitHub Release v2.3.1

Извиняемся за неудобства: в **2.3.0** на части компьютеров с **Windows PowerShell 5.1** не загружался C#‑движок TLS/трассировки, из‑за чего появлялась ошибка в статусной строке и скан по **Enter** не доходил до проверок.

**2.3.1** — исправление совместимости встроенного C# с **PowerShell 5.1** (и проверка на **PowerShell 7**). Рекомендуем обновиться: **`YT-DPI.bat`** или пункт **`[U]`** после появления релиза на GitHub, либо ручная замена **`YT-DPI.ps1`** / пары **`.bat` + `.ps1`** из Releases.

---

# YT-DPI v2.3.0

**Сравнение в Git:** [2.2.3…2.3.0](https://github.com/Shiperoid/YT-DPI/compare/2.2.3...2.3.0) (после публикации тега `2.3.0`; до тега см. [2.2.3…master](https://github.com/Shiperoid/YT-DPI/compare/2.2.3...master)).

Дальше по разделу **v2.3.0** изменения сгруппированы по смыслу: **добавлено**, **обновлено**, **исправлено**, **быстрее и меньше нагрузки на консоль**, затем отладка, проект и релиз.

### Упаковка и запуск (Windows)

* **Лаунчер + `YT-DPI.ps1`:** монолитный огромный `YT-DPI.bat` убран из основной поставки. **`YT-DPI.bat`** только вызывает `pwsh` / `powershell` и при отсутствии рядом **`YT-DPI.ps1`** **однократно** подтягивает его с GitHub (проверка размера). **Зачем:** меньше ложных срабатываний антивирусов и проще выкладывать обновления логики без пересборки «батника на тысячи строк».
* **Обновление [U]:** обновляется файл, с которого запустились (`.bat` или `.ps1`), с проверкой целостности; после успеха докачивается **вторая** часть комплекта рядом, чтобы не остаться с половиной набора.

### Добавлено

* **Ленивая загрузка C#:** TLS‑сканер и код трассировки подгружаются **перед сканом** или **перед Deep Trace**, а не всегда при старте скрипта.
* **Проверка интернета и подготовка скана:** перед запуском скана — быстрый тест доступности сети и подготовка целей; в статус-баре отображаются этапы («проверка сети», загрузка движка и т.п.).
* **Режим TLS в скане:** в настройках **[S]** пункт **3** — цикл **Auto → только TLS 1.2 → только TLS 1.3**; значение передаётся в воркеры (**`TlsMode`**).
* **Отладочный лог из UI:** пункты **[S]** **4–5** — запись в **`YT-DPI_Debug.log`** и опционально полные идентификаторы в заголовке; переменные **`YT_DPI_DEBUG`** и **`YT_DPI_DEBUG_IDENTIFIERS`**; **глобальный mutex** на файл и безопасная запись из **runspace’ов** через общее имя mutex.
* **Меню прокси [P]:** навигация **цифрами**, выход **0** / **Esc**, ввод адреса **строкой** с отменой **Esc**, подтверждение очистки истории **Y/N**, перерисовка при **ресайзе** без залипания ввода.
* **Справка [H]:** **пять** страниц, временное расширение буфера консоли и восстановление высоты после выхода; листание **N** / **P** и стрелками; выход по **Enter** / **Esc** или по **любой другой клавише** (удобно при неверной раскладке).
* **Сводка в статус-баре:** при отсутствии отдельного сообщения показывается **итог последнего скана** по числу вердиктов (AVAILABLE, THROTTLED, DPI, IP BLOCK и т.д.).
* **Прогресс скана в статус-баре:** полоска процента во время сбора результатов и фазы раскрытия (совместно с троттлингом — см. раздел про производительность).
* **Частичный «водопад»:** при повторном скане обновление в основном изменившихся строк и колонки LAT; выборочная перерисовка через **`Write-ResultLine`** / **`Write-ResultLatency`**.
* **`SynchronousProgress` в Deep Trace:** синхронная отдача текста прогресса вместо **`System.Progress`** в условиях pw7 без подходящего **`SynchronizationContext`**.
* **Эвристика «ложного IPv6»:** при **HasIPv6** и полном наборе строк **IP BLOCK** — сброс флага IPv6 и удаление IPv6 из DNS‑кэша.
* **Меню [S]:** пункт **1–2** (как ранее: приоритет IP, сброс кэша) дополнены описанными выше пунктами **3–5**; ввод с **`Read-MenuKeyOrResize`** при ресайзе.
* **Bash (`YT-DPI.sh`):** те же **физические** клавиши при **русской** раскладке, что и латинские пункты меню (в т.ч. при прерывании скана).

### Обновлено

* **Главный экран:** там, где нужно, **`Draw-UI`** с **`ClearScreen=$false`** — перерисовка **поверх** ячеек без полного **`[Console]::Clear`**, меньше мерцания.
* **Ресайз:** раздельная логика для простоя и **фазы скана** (порог > 1 колонка/строка), меньше ложных полных перерисовок из‑за дрожания **`WindowWidth`** / **`Height`**.
* **Старт консоли:** UTF-8, скрытый курсор, уменьшенный **`CursorSize`**.
* **Главный цикл:** раз в секунду подтягивается **NetInfo** после фонового job **без блокировки** ожидания клавиши.
* **Фаза сбора скана:** вместо анимации **каждой** строки каждый кадр (v2.2.3) — прогресс в статус-баре и обновление строк по завершении воркеров.
* **Таблица:** динамические позиции колонок и пересчёт ширины IP после скана для длинных **IPv6**.
* **Пул Runspace:** без изменения модели — **8…24** воркеров (`min(CPU×3, 24)`), с прокси потолок **12**, не больше числа целей.

### Исправлено

* **`Draw-UI` и тип результата:** если в третий параметр передали **boolean** вместо массива результатов, выполняется сброс в **`$null`** — раньше ломалась **только первая** строка таблицы.
* **Deep Trace в PowerShell 7:** снижен риск падений при выводе прогресса из фоновых потоков (переход на **`SynchronousProgress`**).
* **Прокси:** после **неудачного** полного теста конфигурация **восстанавливается** из снимка — не остаётесь с нерабочим прокси.
* **Диагностика прокси:** поэтапные таймауты и сообщения (**`Wait-TcpBeginConnectWithStatus`** и связанная логика).

### Быстрее и меньше нагрузки на консоль

* **Холодный старт:** отложенный **`Add-Type`** до первого использования TLS / трассировки.
* **Фаза сбора скана:** нет постоянной перерисовки LAT и индикатора «SCANNING» по всем строкам на каждом кадре.
* **Троттлинг статус-бара:** ограничение частоты обновления строки прогресса по времени и «ведрам» процента.
* **Кеш `Draw-StatusBar`:** нижние строки перерисовываются только при смене содержимого (**`StatusFeedbackCacheKey`** / **`StatusControlsCacheKey`**).
* **Частичное обновление строк результатов:** **`Write-ResultLatency`** и выборочный **`Write-ResultLine`** уменьшают число операций записи при повторных сканах.

### Отладка и приватность

* По умолчанию в заголовке лога **обезличены** пути и часть метаданных; развернутый заголовок — пункт **[S]** **5** или **`YT_DPI_DEBUG_IDENTIFIERS`**.
* **Ротация** лога при превышении размера.

### Проект

* Вспомогательные скрипты в **`tools/`**; **`img/`**, **`tools/YT-DPI-algorithm`**, **`.editorconfig`** / **`.gitattributes`** по мере развития репозитория.

### Релиз на GitHub (v2.3.0)

Для Windows скачайте **`YT-DPI.bat`** и **`YT-DPI.ps1`** в **одну папку**, запускайте **`YT-DPI.bat`**. Для Linux / macOS / Git Bash / Entware — **`YT-DPI.sh`**.

---

# YT-DPI v2.2.3

По сравнению с **v2.2.2** в этой версии в первую очередь обновлена bash-версия и сопутствующие материалы проекта.

### Bash (`YT-DPI.sh` — Git Bash, Linux, Entware)

* **Вердикт THROTTLED:** отдельный жёлтый статус, когда по TLS видно частичное ограничение (типичный сценарий «троттлинга» вместо полного блока).
* **Паритет с Windows-версией:** те же цели проверки, логика настроек и поведение интерфейса, что у актуального `YT-DPI.bat`.
* **IPv6:** более надёжная детекция и расширенная колонка таблицы под длинные адреса.
* **Latency и анимация:** исправлен расчёт задержки, снижены лаги анимации строки LAT в Git Bash.
* **Entware / OpenWrt:** совместимый способ запуска и приведение комментариев в скрипте к нормальному виду.
* **Режимы TLS:** при успешном HTTP учитывается выбранный режим TLS 1.2 / 1.3 / авто при выводе итогового вердикта.

### Windows (`YT-DPI.bat`)

* Номер версии **2.2.3** и сопутствующие правки стабильности и отображения (в том числе корректная работа с кириллицей и символами Unicode в консоли Windows PowerShell 5.1 при типовых настройках кодировки).

### Проект

* Обновления **README**, ссылка на поддержку автора, мелкие исправления геоданных провайдера (ISP).

### Релиз на GitHub (v2.2.3)

В [релизе](https://github.com/Shiperoid/YT-DPI/releases/tag/2.2.3) в **Assets** доступны два файла:

* **`YT-DPI.bat`** — Windows (один файл: обёртка и сценарий PowerShell внутри, как в прошлых релизах).
* **`YT-DPI.sh`** — Bash для Linux, macOS, Git Bash (Windows) и Entware на роутерах; зависимости и запуск описаны в **README.md** (раздел «Установка»).

**Полный список коммитов:** [2.2.2…2.2.3](https://github.com/Shiperoid/YT-DPI/compare/2.2.2...2.2.3)

---

# YT-DPI v2.0

В версии 2.0 утилита полностью переписана. Инструмент переведен с последовательного сканирования на многопоточную архитектуру, а также получил полностью новый интерактивный консольный интерфейс. Основной упор в обновлении сделан на скорость работы, детализацию тестируемых протоколов и анализ сети в реальном времени.

### Основные нововведения

* **Многопоточный движок (RunspacePools):** Сканирование больше не выполняется последовательно. Утилита одновременно запускает до 20 независимых фоновых потоков, что сократило общее время проверки всех доменов с ~30 до 2-3 секунд.
* **Детальное разделение протоколов:** Вместо объединения всего TLS-трафика в один результат, теперь отдельно тестируются **открытый HTTP (порт 80)**, **TLS 1.2 (порт 443)** и **TLS 1.3 (порт 443)**. Это позволяет точно определить, на каком уровне шифрования ТСПУ обрывает соединение.
* **Детекция тихих дропов (DRP):** Добавлено точное распознавание ситуаций, когда DPI или файрвол отбрасывает пакеты (blackhole) без отправки ответа. Теперь утилита отличает зависшее по таймауту TLS-рукопожатие (`DRP`) от активного сброса соединения (`RST`).
* **Новый интерактивный интерфейс:** Статичный дашборд с абсолютным позиционированием курсора (без прокрутки консоли). Во время работы отображаются динамические эффекты сканирования: поочередный подбор октетов IP-адреса, перебор Hex-байтов на тестируемых портах и изменяющийся пинг.
* **Живая панель телеметрии:** В правом верхнем углу добавлена панель, которая в реальном времени (в процессе сканирования) обновляет количество активных потоков (JOBS), потребление ОЗУ (RAM) и счетчики: заблокированные домены (BLOCKS), сброшенные пакеты (RST) и успешные подключения (CLEAN).
* **Расширенная сетевая аналитика:** Перед каждым сканированием утилита автоматически определяет и выводит на экран текущий активный **DNS**, **имя провайдера (ISP)**, **страну/город** и ближайший **узел Google CDN**.

### Улучшения и исправления

* **Мгновенная отмена сканирования:** Обработка нажатий `Q` или `ESC` теперь использует асинхронную остановку потоков (`BeginStop`). Интерфейс разблокируется моментально, не дожидаясь сетевых таймаутов зависших соединений.
* **Защита от зависаний консоли:** При запуске утилита через вызов C# API автоматически отключает «Режим быстрого редактирования» (QuickEdit Mode) в Windows. Теперь случайное выделение текста мышью в консоли не будет ставить выполнение скрипта на паузу.
* **Умное определение ОС для TLS 1.3:** Windows 7 и старые сборки Windows 10 не поддерживают TLS 1.3 на уровне системы. Утилита теперь распознает это ограничение ОС и выводит статус `N/A` (Недоступно) вместо ложного срабатывания `RST` (Блокировка).
* **Встроенная справка:** Добавлена кнопка `[H]`, открывающая мини-гайд с расшифровкой статус-кодов (OK, RST, DRP, N/A, FAIL) и вердиктов прямо внутри приложения, без необходимости переходить в браузер.
* **Устранено моргание экрана:** Механизм рендеринга интерфейса оптимизирован. Данные обновляются поверх старых значений без полной очистки консоли (`Clear-Host`), что убрало неприятное мерцание при повторных запусках сканирования.

### Расшифровка статус-кодов
* **OK:** Соединение установлено, TLS-рукопожатие завершено успешно.
* **RST:** Соединение принудительно разорвано (TCP Reset) оборудованием DPI/ТСПУ.
* **DRP:** Соединение "тихо" отброшено (Blackhole), TLS-рукопожатие прервано по таймауту.
* **N/A:** Протокол не поддерживается вашей ОС (например, TLS 1.3 на Windows 7).
* **--** / **FAIL:** Ошибка сокета или полная недоступность IP-адреса.

---

### Установка и использование
1. Скачайте файл **`YT-DPI-Scanner-v2.0.bat`** из раздела Assets ниже.
2. Запустите файл (права администратора не требуются).
3. Нажмите `[ENTER]` для запуска сканирования или `[H]` для чтения справки.

*Отказ от ответственности: Данный инструмент предназначен исключительно для диагностических и образовательных целей. Он помогает определить методы блокировки, но сам по себе не является средством обхода ограничений.*


# YT-DPI v2.1.3

Версия 2.1.3 превращает утилиту из простого многопоточного сканера в профессиональный инструмент сетевой аналитики. Основной упор сделан на сохранение состояния, продвинутую работу с прокси и мгновенный запуск.

### 🚀 Производительность и Движок
*   **Система постоянного кэширования:** Все данные сети (Провайдер, CDN, DNS-записи) теперь сохраняются в `%LOCALAPPDATA%`. Повторные запуски происходят **мгновенно** (< 1 сек), так как интерфейс отрисовывается из кэша.
*   **Тайминги на основе Ticks:** Переход на хранение времени в системных тиках (целые числа). Это полностью решило проблему ошибок парсинга даты на разных версиях Windows и локалях.
*   **Отрисовка без мерцания:** Логика UI переписана на "один проход". Раздражающее моргание экрана при старте сканирования полностью устранено.
*   **Умный Dual-Stack (IPv6):** Добавлена полноценная поддержка IPv6. Скрипт определяет наличие IPv6-интернета и приоритезирует его, позволяя найти провайдеров, которые блокируют YouTube только на IPv4.

### 🛡️ Работа с Прокси и Аналитика
*   **Универсальный прокси-движок:** Полная поддержка **SOCKS5** и **HTTP** прокси с поддержкой авторизации.
*   **Автоопределение протокола:** Вам больше не нужно указывать тип прокси. Просто введите `IP:PORT` (например, `127.0.0.1:1080`), и скрипт сам подберет нужный метод рукопожатия.
*   **Честная диагностика через прокси:** Исправлен баг "ложноположительных" зеленых результатов при работе через прокси. Теперь TLS-проверка проводится максимально строго — вы увидите реальную картину работы вашего средства обхода.
*   **Тестер прокси:** По нажатию `[T]` запускается пошаговая диагностика: от доступности хоста до замера задержки (Latency).

### 🛠️ Стабильность и Инструменты
*   **Переработанный Апдейтер:** Новый механизм обновления через изолированные временные процессы. Это исключает ошибки "отказано в доступе" и гарантирует корректную замену файла.
*   **Deep Trace (L4 Traceroute):** Продвинутая трассировка на уровне TCP. Позволяет найти "точку цензуры" (ТСПУ/DPI) на маршруте через инкремент TTL.
*   **Потокобезопасный DNS-кэш:** Найденные IP-адреса синхронизируются между всеми 20+ потоками. Это сводит нагрузку на системный DNS к нулю при повторных проверках.
*   **Техническая документация:** Встроенная справка `[H]` превращена в полноценное руководство, объясняющее работу QUIC, SNI, шифрования Kyber и GGC.

---

### Installation / Установка
1. Download **`YT-DPI.bat`** / Скачайте файл.
2. Run (No Admin required) / Запустите (права админа не требуются, но рекомендуются для Deep Trace).
3. Press `[ENTER]` to Scan.

*Disclaimer: Diagnostic purposes only. / Только для целей диагностики.*
