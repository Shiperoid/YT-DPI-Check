# CI runs this file from the PR merge commit (branch protection applies to that run).
# If you cannot merge a workflow-only fix: push once to the default branch, or
# temporarily relax required checks / use admin bypass (repo Settings → Branches).
name: release-gate

on:
  push:
    paths:
      - 'YT-DPI.ps1'
      - 'tools/release-gate.ps1'
      - 'tools/release-gate-addtype.ps1'
      - 'tools/smoke-yt-dpi-engines.ps1'
      - '.github/workflows/release-gate.yml'
      - 'CHANGELOG_ru.md'
      - 'README.md'
  # No paths filter: branch protection requires gate on every PR; path-only triggers
  # left checks stuck at "Waiting for status to be reported".
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  # Required when merging via a merge queue; otherwise checks stay "Expected".
  merge_group:
    types: [checks_requested]
  # Escape hatch if checks are stuck: Actions → release-gate → Run workflow.
  workflow_dispatch:

jobs:
  gate:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, windows-latest]
        # Pinned + latest: bump minors periodically for reproducibility.
        pwsh_version: ['7.2.23', '7.4.15', 'latest']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      # PowerShell/setup-powershell is not a resolvable public action; use PSModule/install-powershell.
      - name: Install PowerShell (pwsh on PATH for gate)
        uses: PSModule/install-powershell@v1
        with:
          Version: ${{ matrix.pwsh_version }}

      - name: Run release gate (orchestrator = PS 5.1; children = 5.1 x64/WOW64 + all pwsh candidates)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          & (Join-Path $env:GITHUB_WORKSPACE 'tools\release-gate.ps1') -RepoRoot $env:GITHUB_WORKSPACE
